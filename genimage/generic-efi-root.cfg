image %image% {
	hdimage {
		partition-table-type = "gpt"
	}

	partition efi {
		partition-type-uuid = esp
		image = "%efi%"
		size = 64M
	}

	partition root {
		partition-type-uuid = linux
		image = "%root%"
	}
}
